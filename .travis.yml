language: cpp

compiler: gcc
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-5
      env: COMPILER=g++-5

script:
	- cmake -DCMAKE_CXX_COMPILER=$COMPILER .. && make